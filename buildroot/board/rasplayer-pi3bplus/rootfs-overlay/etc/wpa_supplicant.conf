ctrl_interface=/var/run/wpa_supplicant
update_config=1
country=AT

# Add a network={...} block during controlled device provisioning. Keep Wi-Fi
# credentials out of the repository and released image archives.
